name: kennady_photo
description: "Kennady Photo AI App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  share_plus: ^10.0.0
  path_provider: ^2.1.3

flutter:
  uses-material-design: true
